version: '3.0'
services:
  vader:
    build: .
    image: docker.pkg.github.com/j718/vim-pandoc-wiki/vader:latest
    container_name: vader
    volumes:
      - .:/vim-pandoc-wiki
    command:
      - /bin/bash
      - -c
      - | 
        vim -Nu /vim-pandoc-wiki/test/.vimrc -c 'Vader! vim-pandoc-wiki/test/tests.vader' > /dev/null

